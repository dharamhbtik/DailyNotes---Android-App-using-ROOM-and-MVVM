<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/back_shade1"
    tools:context=".activities.AddEditNoteActivity">


    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/et_note_title"
        android:hint="Enter Note Title"
        android:textColor="@color/white"
        android:padding="10dp"
        android:background="@drawable/custom_txt_back"
        android:textColorHint="@color/white"
        android:layout_margin="10dp"></EditText>

    <EditText
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/et_note_description"
        android:hint="Enter Note Description"
        android:textColor="@color/white"
        android:textColorHint="@color/white"
        android:gravity="start|top"
        android:inputType="textMultiLine"
        android:singleLine="false"
        android:layout_margin="10dp"
        android:background="@drawable/custom_txt_back"
        android:layout_above="@id/btn_add_update_note"
        android:layout_below="@id/et_note_title"></EditText>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_add_update_note"
        android:layout_margin="20dp"
        android:layout_alignParentBottom="true"
        android:text="Button"
        android:background="@drawable/custom_button_back"
        android:textAllCaps="false"></Button>
</RelativeLayout>